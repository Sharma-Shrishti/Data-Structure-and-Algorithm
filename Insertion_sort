def insertion_sort(arr):
    for i in range(1,len(arr)):
        temp=arr[i]
        ptr=i
        
        while (ptr>0 and temp<arr[ptr-1]):
            arr[ptr]=arr[ptr-1]
            ptr=ptr-1
        arr[ptr]=temp
    return arr

arr=[9,2,4,1,10,6,98]
insertion_sort(arr)
